version: "3"

services:

  mysql:
    image: mysql:8
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=joinEcommerce
      - MYSQL_USER=joinEcommerce
      - MYSQL_PASSWORD=joinEcommerce